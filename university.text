//Primo Punto!!!

SELECT *
FROM `students`
WHERE YEAR(`date_of_birth`) = 1990;

// Secondo Punto!!!

SELECT * 
FROM `courses`
WHERE `cfu` > 10;

// Terzo Punto!!!

SELECT * 
FROM `students`
WHERE YEAR(`date_of_birth`) > 1993;